import category from './category'
import faqs from './faqs'
import order from './order'
import orderItem from './orderItem'
import product from './product'

export const schemaTypes = [product, category, order, orderItem, faqs]
